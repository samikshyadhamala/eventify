export type BranchAdmin = {
    email: string
    branch_name: string
    location: string
    user_id: string
    branch_id: string
}

export type Branch = {
    branch_id: string
    branch_name: string
}

export type User = {
    fid: string
    email: string
    imageUrl: string
}